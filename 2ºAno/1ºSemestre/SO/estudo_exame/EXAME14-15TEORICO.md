# SO 14-15

# 1
```
O Firmware é um software armazenado de forma permanente em memória flash.
Este contém rotinas utilitarias para controlar aspetos do equipamento e rotinas para inicializar esse equipamento e colocá-lo num estado inicial coerente: chamada Basic Input Output System(BIOS)

São fixas e estão em memória não volátil.
```

# 2
```
No ficheiro /etc/shadow
```

# 3
```
O sudo é um dos programas que tem o bit setuid ligado, ou seja, quando executamos um ficheiro com sudo, estamos a executar em nome do root uma vez que o ficheiro sudo pertence ao root.

Pode executar o comando makesys sempre, bastante usar o comando "sudo"
```

# 4
```
Nenhuma das anteriores está certa
```

# 5
```
```

# 7
```
É bem organizada e estável
```

# 10
```
Fechamos o STDIN e o STDOUT e ao fazer open do ficheiro em O_WRONLY estamos a redirecionar o printf("comando")(que iria mandar para o STDOUT) para o ficheiro.

O utilizador não vê nada no ecrã e o ficheiro "abc.txt" contém a palavra "comando"
```

# 11
```
Deteção de um endereço inválido
```

# 12
```
É lançado um fork dentro do if, no entanto quando se faz isto não é certo se é o pai ou o filho a serem executados primeiro, logo aparecem varios 0 1 e 2.

A opção mais correta será a b
```

# 14
```
Tanto a memória paginada como a memória segmentada permitem definir proteções a nivel do que é que se pode fazer com cada zona de memória (nao sei se está certo)
```
# 15

```
Max pages per process * Size of a page = Max memory available to a process

Logo é a A
```
# 16

```
2^12 = 4096
8196/4096 = 2 (resto 4)
Vamos ao indice 2 e somamos 4 = 16388
```
